name = "Lyl66655/moonbit-easings"

version = "0.1.0"

readme = "README.mbt.md"

repository = ""

license = "MIT"

keywords = [ "animation", "easing", "tween", "bezier", "keyframe", "media" ]

preferred_target = "wasm"

description = "A dependency-free MoonBit motion toolkit for media timelines, easing curves, cubic Bezier inversion, and scalar keyframes."
